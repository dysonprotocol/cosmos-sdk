# 12-2 Copy `cosmos-sdk/x/nft` to `x/nft` in the Dyson Protocol codebase

[Back to task list](./tasks.md) | [View Backlog](../backlog.md#user-content-12)

## Analysis
- **Purpose:** Establish the Dyson-native NFT module by copying the Cosmos SDK NFT module to the new location.
- **Non-functional Considerations:**
  - Security: Ensure file permissions and access remain correct after copying.
  - Performance: Copy operation should be atomic to avoid partial states.
  - Observability: Not applicable.
- **Business Rules:**
  - No changes to business logic or functionality during the copy.

## Design
- **Technical Specifications:**
  - Use a reliable method to copy all files and subdirectories from `cosmos-sdk/x/nft` to `x/nft`.
  - Preserve file structure and permissions.
- **Architectural Compliance:**
  - The new module must fit Dyson Protocol's module layout.

## Testing
- **Test Cases:**
  - Verify all files and subdirectories are present in `x/nft`.
  - Confirm no files are missing or corrupted.
- **Test Data Requirements:**
  - Not applicable.

## Change Plan
- **Files to Modify:**
  - Create all files in `x/nft` as a copy of `cosmos-sdk/x/nft`.
- **Implementation Approach:**
  - Perform a directory copy and verify integrity post-copy. 